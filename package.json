{
  "private": true,
  "scripts": {
    "build": "rimraf dist && node build.mjs && es-check es3 dist/**/*.js --checkFeatures"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "es-check": "^9.3.1",
    "jsdom": "^26.1.0",
    "rimraf": "^6.0.1",
    "terser": "^5.44.0"
  }
}
